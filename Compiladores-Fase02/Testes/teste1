function imprima(before: Int, valor: Int, after: String, next: Boolean) {
  var i : Int;
  var j : Int;
  i = 0;
  while i < before {
    write("");
    i = i + 1;
  }
  writeln("");
  writeln(valor);
  writeln(after);
}